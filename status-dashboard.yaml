database:
  host: localhost
  user: root
  password: password
  database: status_dashboard
  status_table: dashboard_events

services:
  auth_saml: Auth Saml
  auth_oidc: Auth OIDC
  privacyidea: MFA
